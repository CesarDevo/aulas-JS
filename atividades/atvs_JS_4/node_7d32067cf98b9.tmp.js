let n;

do {
    // n = Number(prompt("Digite sua idade: "));
    n = "e";

} while ( (n < 0) || (n)); // repete enquanto for string ou um número negativo

// alert(`Sua idade é ${n}`);

console.log(n);